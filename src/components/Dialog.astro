---

const { title, id, showSubmit = true, showCloseButton = true } = Astro.props;
---

<div
  data-dialog={id}
  class="fixed inset-0 z-[9999] flex items-center justify-center bg-black/50 backdrop-blur-sm hidden"
>
<div class="dialog-inner bg-[var(--color-background)] rounded-xl shadow-xl max-w-2xl w-[90vw] sm:w-full p-6 sm:p-12 relative max-h-[80vh] flex flex-col gap-4">
      {showCloseButton && (
      <button
        type="button"
        class="absolute top-3 right-3 text-gray-500 hover:text-gray-700 z-10"
        data-close-dialog
      >
        ✕
      </button>
    )}

{title && <h2 class="text-xl font-semibold mb-6 flex-shrink-0">{title}</h2>}
    <!-- Usa <slot /> en lugar de {children} -->
        <!-- Usa <slot /> en lugar de {children} -->
        <div class="flex-1 overflow-y-auto">
          <div class="flex flex-col gap-3 sm:gap-4">
            <slot />
          </div>
        </div>

  </div>
</div>
